name: Release

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  release:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: davegarvey/grubble@v5.3.0
        with:
          push: true
          tag: true
          package-files: "package.json"
          preset: node
          create-pr: true
          auto-merge: true
