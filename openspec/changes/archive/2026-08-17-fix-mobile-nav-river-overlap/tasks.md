## 1. CSS changes

- [x] 1.1 Add `padding-top: 40px` to `.river` inside `@media (max-width: 768px)` block
- [x] 1.2 Add `padding-top: 44px` to `.river` inside `@media (any-pointer: coarse)` block

## 2. Verification

- [x] 2.1 Run `npm run typecheck` to confirm no type errors
- [x] 2.2 Run `npm run lint` to confirm no lint errors
- [x] 2.3 Verify visually in mobile viewport that river content is no longer obscured by the TopBar
