## 1. Zone positioning

- [x] 1.1 Publish the reader pane's left offset as `--reading-left` and position the previous-article zone from it. Verify by hit-testing the sidebar with an article open in the desktop workspace, and by toggling focus mode.

## 2. Specification sync

- [x] 2.1 Modify the desktop chevron requirement, validate the change, and archive it so the requirement is synced to the main spec.
